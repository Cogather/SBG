# [模块名称] - 模块间接口契约文档

## 1. 概述

本文档定义了[模块名称]与其他模块之间的接口契约。[模块名称]是[系统名称]的[定位描述]，对外通过`[核心接口名]`接口提供[核心能力描述]。本文档确保模块间的交互规范、明确、无歧义。

---

## 2. 模块依赖关系

```
[模块名称]
│
├── [对外提供] [接口名] 接口
│   ├── 调用方: [调用模块1]
│   ├── 调用方: [调用模块2]
│   └── 调用方: [调用模块3]
│
├── [对外提供] [API/RPC接口名]（如有）
│   └── 调用方: [外部调用方]
│
└── [依赖] 外部模块接口
    ├── [依赖接口1]
    ├── [依赖接口2]
    └── [依赖接口3]
```

---

## 3. 对外提供的接口契约

### 3.1 [接口名]

#### 3.1.1 接口描述

[接口的功能定位和职责说明。]

#### 3.1.2 接口方法定义

##### [方法名]

**功能**: [方法功能描述]

**签名**:
```
[返回类型] [方法名]([参数类型] [参数名], ...)
```

**参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| [参数名] | [类型] | 是/否 | [说明] |

**返回值**: [返回值说明，不存在时的行为]

**异常**:

| 异常类型 | 抛出条件 |
|---------|---------|
| [异常类型] | [触发条件] |

**契约约束**:
- 前置条件：[调用方需满足的条件]
- 后置条件：[调用成功后的状态保证]
- 幂等性：[是/否，说明原因]
- 线程安全：[是/否，说明机制]

**调用方及调用时机**:

| 调用方 | 调用时机 |
|--------|---------|
| [调用方] | [何时调用] |

---

##### [方法名2]

**功能**: [方法功能描述]

**签名**:
```
[返回类型] [方法名2]([参数类型] [参数名], ...)
```

**参数**:

| 参数名 | 类型 | 必填 | 说明 |
|--------|------|------|------|
| [参数名] | [类型] | 是/否 | [说明] |

**返回值**: [返回值说明]

**契约约束**:
- [约束说明]

---

#### 3.1.3 核心数据结构

##### [数据类名]

| 字段名 | 类型 | 必填 | 约束 | 说明 |
|--------|------|------|------|------|
| [字段名] | [类型] | 是/否 | [约束，如唯一/非空/长度限制] | [说明] |

---

### 3.2 [HTTP/RPC 接口名]（如有）

#### 3.2.1 [接口功能名]

```
[HTTP METHOD] [路径]
Content-Type: application/json
```

**请求参数**:

| 参数名 | 位置 | 类型 | 必填 | 说明 |
|--------|------|------|------|------|
| [参数名] | Body/Query/Path | [类型] | 是/否 | [说明] |

**响应体**:
```json
{
    "code": 0,
    "message": "success",
    "data": {}
}
```

**错误码**:

| 错误码 | 含义 | 处理建议 |
|--------|------|---------|
| 0 | 成功 | - |
| [错误码] | [含义] | [处理建议] |

**处理流程**:
```
1. [步骤1]
2. [步骤2]
3. [步骤3]

异常处理: [异常处理策略]
```

---

## 4. 依赖的外部接口契约

### 4.1 [依赖接口名]

#### 4.1.1 接口描述

[该依赖接口的功能说明，以及本模块使用它的目的。]

#### 4.1.2 使用的方法

| 方法签名 | 调用方 | 调用时机 | 说明 |
|---------|--------|---------|------|
| `[返回类型] [方法名]([参数])` | [本模块调用类] | [调用时机] | [说明] |

#### 4.1.3 关键数据结构（如有）

| 字段名 | 类型 | 说明 |
|--------|------|------|
| [字段名] | [类型] | [说明] |

---

### 4.2 [依赖接口名2]（如有）

#### 4.2.1 接口描述

[说明]

#### 4.2.2 使用的方法

| 方法签名 | 调用方 | 调用时机 | 说明 |
|---------|--------|---------|------|
| `[方法签名]` | [调用类] | [时机] | [说明] |

---

## 5. 接口调用关系矩阵

### 5.1 [接口名] 被调用矩阵

| 调用方 \ 方法 | [方法1] | [方法2] | [方法3] |
|--------------|---------|---------|---------|
| [调用方1] | ✓ | - | ✓ |
| [调用方2] | - | ✓ | - |

### 5.2 本模块对外依赖矩阵

| 组件 \ 依赖 | [依赖接口1] | [依赖接口2] | [依赖接口3] |
|------------|------------|------------|------------|
| [组件1] | ✓ | ✓ | - |
| [组件2] | - | ✓ | ✓ |

---

## 6. 异常处理规范

| 层次 | 方法 | 异常场景 | 处理策略 | 返回值 |
|------|------|---------|---------|--------|
| [API层] | [方法名] | 所有异常 | try-catch，不暴露内部细节 | [错误响应] |
| [Service层] | [方法名] | [场景] | [策略，如打印日志并返回默认值] | [返回值] |
| [Service层] | [方法名] | [场景] | [策略，如向上抛出] | - |

---

## 7. 性能要求

| 操作 | 目标响应时间 | 并发要求 | 说明 |
|------|------------|---------|------|
| [操作名] | [如 < 100ms / < 5s] | [如 线程安全 / 幂等] | [说明] |

---

## 8. 版本历史

| 版本 | 日期 | 修改内容 | 作者 |
|-----|------|---------|------|
| 1.0 | YYYY-MM-DD | 初始版本 | [作者] |

---

**文档结束**
