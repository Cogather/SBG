[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "browser-proxy"
version = "0.1.0"
description = "Browser proxy service based on FastAPI and Playwright"
requires-python = ">=3.8"

[project.scripts]
browser_proxy = "browser_proxy.main:main"
