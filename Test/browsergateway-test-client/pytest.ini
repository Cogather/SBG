[pytest]
markers =
    slow: long-running tests (e.g. multi-minute idle)
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
asyncio_default_test_loop_scope = session
testpaths = tests
addopts =
    --junitxml=reports/junit.xml
    --html=reports/report.html
    --self-contained-html
